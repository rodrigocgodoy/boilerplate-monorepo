services:
  postgres:
    image: postgres:16-alpine
    container_name: boilerplate-postgres
    restart: ${RESTART_POLICY:-on-failure:3}
    command: postgres -c max_connections=300
    ports:
      - ${DB_PORT:-5432}:5432
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_DATABASE:-boilerplate}
    volumes:
      - postgresql:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${DB_USER:-postgres}']
      interval: 1s
      timeout: 5s
      retries: 10
    logging:
      options:
        max-size: 100m

  # ─────────────────────────────────────────────────────────────
  # UPGRADE opcional: Redis (cache / sessão secundária do Better Auth /
  # rate-limit distribuído). Descomente este bloco e veja UPGRADES.md.
  # ─────────────────────────────────────────────────────────────
  # redis:
  #   image: redis:7-alpine
  #   container_name: boilerplate-redis
  #   restart: ${RESTART_POLICY:-on-failure:3}
  #   ports:
  #     - '${REDIS_PORT:-6379}:6379'
  #   volumes:
  #     - redis:/data
  #   healthcheck:
  #     test: ['CMD', 'redis-cli', 'ping']
  #     interval: 1s
  #     timeout: 5s
  #     retries: 10
  #   logging:
  #     options:
  #       max-size: 100m

  # ─────────────────────────────────────────────────────────────
  # UPGRADE opcional: MinIO (storage S3-compatível para uploads).
  # Descomente este bloco + minio-init e veja UPGRADES.md.
  # ─────────────────────────────────────────────────────────────
  # minio:
  #   image: minio/minio:latest
  #   container_name: boilerplate-minio
  #   restart: ${RESTART_POLICY:-on-failure:3}
  #   ports:
  #     - '${MINIO_API_PORT:-9000}:9000'
  #     - '${MINIO_CONSOLE_PORT:-9001}:9001'
  #   environment:
  #     MINIO_ROOT_USER: ${S3_ACCESS_KEY:-minioadmin}
  #     MINIO_ROOT_PASSWORD: ${S3_SECRET_KEY:-minioadmin}
  #   volumes:
  #     - minio:/data
  #   command: server /data --console-address ":9001"
  #   healthcheck:
  #     test: ['CMD', 'mc', 'ready', 'local']
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5
  #   logging:
  #     options:
  #       max-size: 100m
  #
  # minio-init:
  #   image: minio/mc:latest
  #   container_name: boilerplate-minio-init
  #   depends_on:
  #     minio:
  #       condition: service_healthy
  #   entrypoint: >
  #     /bin/sh -c "
  #     mc alias set local http://minio:9000 $${MINIO_ROOT_USER:-minioadmin} $${MINIO_ROOT_PASSWORD:-minioadmin};
  #     mc mb local/$${S3_BUCKET:-boilerplate} --ignore-existing;
  #     mc anonymous set download local/$${S3_BUCKET:-boilerplate}/public;
  #     exit 0;
  #     "
  #   environment:
  #     MINIO_ROOT_USER: ${S3_ACCESS_KEY:-minioadmin}
  #     MINIO_ROOT_PASSWORD: ${S3_SECRET_KEY:-minioadmin}
  #     S3_BUCKET: ${S3_BUCKET:-boilerplate}

volumes:
  postgresql:
  # redis:
  # minio:
